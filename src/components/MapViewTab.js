import Button from "./Button";
import "../styles/index.css";

function MapViewTab() {
  return (
    <div>
      <Button primary rounded>
        지도로 보기
      </Button>
    </div>
  );
}

export default MapViewTab;
